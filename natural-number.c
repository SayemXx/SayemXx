#include<stdio.h>
int main(){
    int number;
    printf("Enter the Number :");
    scanf("%d",&number);
    if(number>0){
        printf("The Number is Natural Number");
    }
  //I can't write any code for the input float value. I will fixed this later.
   
   else{
    printf("The number is not a Natural Number");
   }
   return 0;
}